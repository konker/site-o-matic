{{{ generatorWarning }}}

import cf from 'cloudfront';

// Viewer Response Handler
async function handler(event) {
  var request = event.request;
  var response = event.response;

  {{#each subComponents}}
    // ---------------
    {{{ this }}}

  {{/each}}

  return response;
}
