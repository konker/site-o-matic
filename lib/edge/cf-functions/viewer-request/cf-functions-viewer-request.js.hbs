{{{ generatorWarning }}}

// Viewer Request Handler
function handler(event) {
  var request = event.request;

  {{#each subComponents}}
    // ---------------
    {{{ this }}}

  {{/each}}

  return request;
}
